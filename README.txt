moodle-tinymce_wordcount
========================

A Moodle sub-plugin to override the word counting rules in the standard TinyMCE wordcount plugin to make them consistent with Moodle's own word count function.
